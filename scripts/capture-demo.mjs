// Capture real compact desktop screenshots while verifying the Rust-backed graph.
// The end-to-end run writes docs/images/circuit-{dark,light,impact}.png along with
// test evidence. Pass --packaged to capture the current portable release.
await import('./test-desktop.mjs');
